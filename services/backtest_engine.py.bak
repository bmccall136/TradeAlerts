# services/backtest_engine.py

from services.settings_schema import BacktestSettings
from services.trading_helpers import init_backtest_db
from settings import BACKTEST_DB
from services.risk_management import enforce_wash_sale, enforce_settlement

def run_full_backtest(
    settings: BacktestSettings,
    symbols,
    wash_sale=None,
    settlement=None,
    **kwargs
):
    """
    Initialize the backtest DB and run the backtest according to the supplied settings.
    Returns: (trades_list, summary_dict)
    """
    # 1) (Re)create the backtest schema
    init_backtest_db()

    # 2) TODO: implement your backtest logic here
    #    - fetch historical data for `symbols`
    #    - apply indicators per `settings`
    #    - enforce wash_sale(...) and settlement(...) if provided
    #    - record trades into BACKTEST_DB

    # For now, return empty results so it matches the expected signature:
    return [], {}
