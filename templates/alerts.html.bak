{# File: templates/alerts.html #}
{% extends "layout.html" %}
{% block content %}
{# …inside alerts.html, right after `{% block content %}`… #}
<div class="container">
  <div class="d-flex align-items-center position-relative mb-4">
    <!-- Export CSV at absolute left -->
    <a href="{{ url_for('export_alerts') }}"
       class="btn btn-outline-light position-absolute start-0">
      <i class="bi bi-download"></i> Export CSV
    </a>

    <!-- Centered title -->
    <h2 class="text-warning mb-0 mx-auto">Alerts Dashboard</h2>
  </div>
</div>

{# …then the rest of your existing form / table below… #}

<style>
  /* 1) Shrink overall font and padding for alerts table */
  table.alerts-table th,
  table.alerts-table td {
    padding: 2px 4px;
    font-size: 12px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* 2) Fixed layout and full width */
  table.alerts-table {
    width: 100%;
    table-layout: fixed;
    border-collapse: collapse;
  }

  /* 3) Column widths */
  table.alerts-table th:nth-child(1), table.alerts-table td:nth-child(1) { width: 14%; }
  table.alerts-table th:nth-child(2), table.alerts-table td:nth-child(2) { width: 6%; }
  table.alerts-table th:nth-child(3), table.alerts-table td:nth-child(3) { width: 18%; }
  table.alerts-table th:nth-child(4), table.alerts-table td:nth-child(4) { width: 8%; }
  table.alerts-table th:nth-child(5), table.alerts-table td:nth-child(5) { width: 8%; }
  table.alerts-table th:nth-child(6), table.alerts-table td:nth-child(6) { width: 8%; }
  table.alerts-table th:nth-child(7), table.alerts-table td:nth-child(7) { width: 6%; }
  table.alerts-table th:nth-child(8), table.alerts-table td:nth-child(8) { width: %; }
  table.alerts-table th:nth-child(9), table.alerts-table td:nth-child(9) { width: 12%; white-space: normal; }
  table.alerts-table th:nth-child(10), table.alerts-table td:nth-child(10) { width: 5%; }
  table.alerts-table th:nth-child(11), table.alerts-table td:nth-child(11) { width: 3%; }
  table.alerts-table th:nth-child(12), table.alerts-table td:nth-child(12) { width: 3%; }
</style>

<div class="container">
  {# ─── Indicator Settings Form ─── #}
  {% set sma_labels = {10: "Short SMA (10)", 20: "Standard SMA (20)", 50: "Long SMA (50)", 100: "100-Day SMA (100)", 200: "200-Day SMA (200)"} %}
  {% set rsi_len_labels = {7: "Fast RSI (7)", 14: "Std RSI (14)", 21: "Slow RSI (21)", 28: "Very Slow RSI (28)"} %}
  {% set rsi_ob_labels = {70: "Overbought (70)", 75: "Very Overbought (75)", 80: "Extreme Overbought (80)", 85: "Max Overbought (85)"} %}
  {% set rsi_os_labels = {20: "Oversold (20)", 25: "Very Oversold (25)", 30: "Extreme Oversold (30)", 35: "Max Oversold (35)"} %}
  {% set macd_fast_labels = {8: "Fast EMA (8)", 12: "Fast EMA (12)", 16: "Fast EMA (16)"} %}
  {% set macd_slow_labels = {18: "Slow EMA (18)", 26: "Slow EMA (26)", 34: "Slow EMA (34)"} %}
  {% set macd_signal_labels = {6: "Signal SMA (6)", 9: "Signal SMA (9)", 12: "Signal SMA (12)"} %}
  {% set bb_length_labels = {10: "BB Short (10)", 20: "BB Std (20)", 30: "BB Long (30)"} %}
  {% set bb_std_labels = {1.5: "1.5σ", 2.0: "2σ", 2.5: "2.5σ"} %}
  {% set vol_mult_labels = {1.0: "Normal ×1.0", 1.5: "High ×1.5", 2.0: "Very High ×2.0", 2.5: "Extreme ×2.5"} %}
  {% set vwap_labels = {0.0: "No VWAP", 0.5: "≥ $0.5", 1.0: "≥ $1.0", 2.0: "≥ $2.0"} %}

{# templates/alerts.html – replace your existing simulation section with this #}

<form id="simulation-form"
      method="POST"
      action="{{ url_for('start_scanner') }}">

  {# ─── 1) Indicator Toggles ─── #}
  <div class="d-flex flex-wrap align-items-center mb-4">
    {% set toggles = [
      {'name':'sma_on',  'label':'SMA',  'icon':'bi-graph-up-arrow'},
      {'name':'rsi_on',  'label':'RSI',  'icon':'bi-speedometer2'},
      {'name':'macd_on', 'label':'MACD', 'icon':'bi-triangle-fill'},
      {'name':'bb_on',   'label':'BB',   'icon':'bi-bar-chart-fill'},
      {'name':'vol_on',  'label':'Vol',  'icon':'bi-bar-chart-lines'},
      {'name':'vwap_on', 'label':'VWAP', 'icon':'bi-calculator'},
      {'name':'news_on', 'label':'News', 'icon':'bi-newspaper'}
    ] %}
    {% for t in toggles %}
      <div class="form-check form-switch me-3 mb-2">
        <input class="form-check-input"
               type="checkbox"
               id="{{ t.name }}"
               name="{{ t.name }}"
               {% if settings[t.name] %}checked{% endif %}>
        <label class="form-check-label" for="{{ t.name }}">
          <i class="bi {{ t.icon }}"></i> {{ t.label }}
        </label>
      </div>
    {% endfor %}
  </div>

{# ─── 2) Numeric Filters ─── #}
<div class="row g-2 align-items-end mb-4">
  <!-- SMA Length -->
  <div class="col-6 col-md-2">
    <label for="sma_length" class="form-label">
      <i class="bi bi-graph-up-arrow"></i> SMA Length
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="Simple Moving Average"
         data-bs-content="Average of closing prices over the last N periods."></i>
    </label>
    <select id="sma_length" name="sma_length" class="form-select">
      {% for v,l in sma_labels.items() %}
        <option value="{{ v }}" {% if settings.sma_length==v %}selected{% endif %}>{{ l }}</option>
      {% endfor %}
    </select>
  </div>

<!-- RSI Length -->
<div class="col-6 col-md-1 text-center">
  <label for="rsi_len"
         class="form-label d-flex flex-column align-items-center">
    <!-- line 1: the popover icon -->
    <i class="bi bi-info-circle-fill text-muted mb-1"
       data-bs-toggle="popover"
       title="RSI Length"
       data-bs-content="Number of periods used to calculate RSI."></i>
    <!-- line 2: the actual label -->
    <span><i class="bi bi-speedometer2"></i> RSI Len</span>
  </label>
  <select id="rsi_len" name="rsi_len" class="form-select">
    {% for v,l in rsi_len_labels.items() %}
      <option value="{{ v }}" {% if settings.rsi_len==v %}selected{% endif %}>{{ l }}</option>
    {% endfor %}
  </select>
</div>

  <!-- RSI Overbought -->
  <div class="col-6 col-md-1">
    <label for="rsi_overbought" class="form-label">
      OB ≥
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="RSI Overbought Threshold"
         data-bs-content="Alert when RSI rises above this level."></i>
    </label>
    <select id="rsi_overbought" name="rsi_overbought" class="form-select">
      {% for v,l in rsi_ob_labels.items() %}
        <option value="{{ v }}" {% if settings.rsi_overbought==v %}selected{% endif %}>{{ l }}</option>
      {% endfor %}
    </select>
  </div>

  <!-- RSI Oversold -->
  <div class="col-6 col-md-1">
    <label for="rsi_oversold" class="form-label">
      OS ≤
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="RSI Oversold Threshold"
         data-bs-content="Alert when RSI falls below this level."></i>
    </label>
    <select id="rsi_oversold" name="rsi_oversold" class="form-select">
      {% for v,l in rsi_os_labels.items() %}
        <option value="{{ v }}" {% if settings.rsi_oversold==v %}selected{% endif %}>{{ l }}</option>
      {% endfor %}
    </select>
  </div>

  <!-- MACD Fast EMA -->
  <div class="col-6 col-md-1">
    <label for="macd_fast" class="form-label">
      <i class="bi bi-triangle-fill"></i> Fast EMA
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="MACD Fast EMA"
         data-bs-content="Short-term exponential moving average for MACD."></i>
    </label>
    <select id="macd_fast" name="macd_fast" class="form-select">
      {% for v,l in macd_fast_labels.items() %}
        <option value="{{ v }}" {% if settings.macd_fast==v %}selected{% endif %}>{{ l }}</option>
      {% endfor %}
    </select>
  </div>

  <!-- MACD Slow EMA -->
  <div class="col-6 col-md-1">
    <label for="macd_slow" class="form-label">
      Slow EMA
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="MACD Slow EMA"
         data-bs-content="Long-term exponential moving average for MACD."></i>
    </label>
    <select id="macd_slow" name="macd_slow" class="form-select">
      {% for v,l in macd_slow_labels.items() %}
        <option value="{{ v }}" {% if settings.macd_slow==v %}selected{% endif %}>{{ l }}</option>
      {% endfor %}
    </select>
  </div>

  <!-- MACD Signal EMA -->
  <div class="col-6 col-md-1">
    <label for="macd_signal" class="form-label">
      Signal EMA
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="MACD Signal Line"
         data-bs-content="EMA of the MACD line used to generate signals."></i>
    </label>
    <select id="macd_signal" name="macd_signal" class="form-select">
      {% for v,l in macd_signal_labels.items() %}
        <option value="{{ v }}" {% if settings.macd_signal==v %}selected{% endif %}>{{ l }}</option>
      {% endfor %}
    </select>
  </div>

<!-- BB Length -->
<div class="col-6 col-md-1 text-center">
  <label for="bb_length"
         class="form-label d-flex flex-column align-items-center">
    <i class="bi bi-info-circle-fill text-muted mb-1"
       data-bs-toggle="popover"
       title="Bollinger Band Length"
       data-bs-content="Window size for the BB moving average."></i>
    <span><i class="bi bi-bar-chart-fill"></i> BB Length</span>
  </label>
  <select id="bb_length" name="bb_length" class="form-select">
    {% for v,l in bb_length_labels.items() %}
      <option value="{{ v }}" {% if settings.bb_length==v %}selected{% endif %}>{{ l }}</option>
    {% endfor %}
  </select>
</div>

  <!-- Bollinger Band Std Dev -->
  <div class="col-6 col-md-1">
    <label for="bb_std" class="form-label">
      BB Std Dev
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="BB Standard Deviation"
         data-bs-content="Number of standard deviations around the moving average."></i>
    </label>
    <select id="bb_std" name="bb_std" class="form-select">
      {% for v,l in bb_std_labels.items() %}
        <option value="{{ v }}" {% if settings.bb_std==v %}selected{% endif %}>{{ l }}</option>
      {% endfor %}
    </select>
  </div>

  <!-- Volume Multiplier -->
  <div class="col-6 col-md-1">
    <label for="vol_multiplier" class="form-label">
      <i class="bi bi-bar-chart-lines"></i> Vol Multiplier
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="Volume Spike Multiplier"
         data-bs-content="Volume must exceed this multiple of its 5-day average."></i>
    </label>
	  <select id="vol_multiplier" name="vol_multiplier" class="form-select">
		{% for v,l in vol_mult_labels.items() %}
		  <option value="{{ v }}"
			{% if settings.vol_multiplier|default(2.0) == v %}selected{% endif %}>
			{{ l }}
		  </option>
		{% endfor %}
    </select>
  </div>

  <!-- VWAP Threshold -->
  <div class="col-6 col-md-1">
    <label for="vwap_threshold" class="form-label">
      <i class="bi bi-calculator"></i> VWAP Thresh
      <i class="bi bi-info-circle-fill text-muted ms-1"
         data-bs-toggle="popover"
         title="VWAP Threshold"
         data-bs-content="Price must be at least this dollar amount above VWAP."></i>
    </label>
	  <select id="vwap_threshold" name="vwap_threshold" class="form-select">
		{% for v,l in vwap_labels.items() %}
		  <option value="{{ v }}"
			{% if settings.vwap_threshold|default(0.5) == v %}selected{% endif %}>
			{{ l }}
		  </option>
		{% endfor %}	
    </select>
  </div>
</div>


{# ─── 3) “Extra” Indicator Toggles ─── #}
<div class="row mb-4">
  <!-- RSI Slope -->
  <div class="col-auto">
    <div class="form-check">
      <input class="form-check-input" type="checkbox"
             id="rsi_slope_on" name="rsi_slope_on"
             {% if settings.rsi_slope_on %}checked{% endif %}>
      <label class="form-check-label text-warning d-flex align-items-center" for="rsi_slope_on">
        📈 Require RSI Slope &gt; 0
        <i class="bi bi-info-circle-fill ms-1 text-muted"
           data-bs-toggle="popover"
           title="RSI Slope"
           data-bs-content="Require that the slope (change) of the RSI indicator is positive.">
        </i>
      </label>
    </div>
  </div>

  <!-- MACD Histogram -->
  <div class="col-auto">
    <div class="form-check">
      <input class="form-check-input" type="checkbox"
             id="macd_hist_on" name="macd_hist_on"
             {% if settings.macd_hist_on %}checked{% endif %}>
      <label class="form-check-label text-warning d-flex align-items-center" for="macd_hist_on">
        📊 Require MACD Histogram &gt; 0.5
        <i class="bi bi-info-circle-fill ms-1 text-muted"
           data-bs-toggle="popover"
           title="MACD Histogram"
           data-bs-content="Require that the MACD histogram bar is above 0.5 to confirm momentum.">
        </i>
      </label>
    </div>
  </div>

  <!-- BB Breakout -->
  <div class="col-auto">
    <div class="form-check">
      <input class="form-check-input" type="checkbox"
             id="bb_breakout_on" name="bb_breakout_on"
             {% if settings.bb_breakout_on %}checked{% endif %}>
      <label class="form-check-label text-warning d-flex align-items-center" for="bb_breakout_on">
        🚀 Require BB Breakout
        <i class="bi bi-info-circle-fill ms-1 text-muted"
           data-bs-toggle="popover"
           title="Bollinger Band Breakout"
           data-bs-content="Require the price to break above the upper Bollinger Band.">
        </i>
      </label>
    </div>
  </div>
</div>

{# ── Fixed Volatility Filters ── #}
<div class="row g-4 align-items-center mb-4 text-nowrap">
  <!-- ATR14 ≥ 1.4 -->
  <div class="col-auto d-flex align-items-center me-5">
    <div class="form-check me-2">
      <input class="form-check-input" type="checkbox"
             id="atr14_on" name="atr14_on" checked>
    </div>
    <label class="form-check-label text-warning d-flex align-items-center me-2" for="atr14_on">
      ATR14 ≥ 1.4
      <i class="bi bi-info-circle-fill ms-1 text-muted"
         data-bs-toggle="popover"
         title="ATR14 (Average True Range)"
         data-bs-content="Measures volatility over the last 14 periods.">
      </i>
    </label>
    <input type="hidden" name="atr_threshold" value="1.4">
  </div>

  <!-- ATR % ≥ 0.7 -->
  <div class="col-auto d-flex align-items-center me-5">
    <div class="form-check me-2">
      <input class="form-check-input" type="checkbox"
             id="atr_pct_on" name="atr_pct_on" checked>
    </div>
    <label class="form-check-label text-warning d-flex align-items-center me-2" for="atr_pct_on">
      ATR % ≥ 0.7 %
      <i class="bi bi-info-circle-fill ms-1 text-muted"
         data-bs-toggle="popover"
         title="ATR14/Close %"
         data-bs-content="ATR14 as a percentage of the previous close price.">
      </i>
    </label>
    <input type="hidden" name="atr_pct" value="0.7">
  </div>

  <!-- Range % ≥ 1.2 -->
  <div class="col-auto d-flex align-items-center">
    <div class="form-check me-2">
      <input class="form-check-input" type="checkbox"
             id="range_on" name="range_on" checked>
    </div>
    <label class="form-check-label text-warning d-flex align-items-center me-2" for="range_on">
      Range % ≥ 1.2 %
      <i class="bi bi-info-circle-fill ms-1 text-muted"
         data-bs-toggle="popover"
         title="Daily Range %"
         data-bs-content="(High − Low) divided by previous close, as a percentage.">
      </i>
    </label>
    <input type="hidden" name="range_pct" value="1.2">
  </div>
  <!-- Gap % ≥ 1.0 -->
  <div class="col-auto d-flex align-items-center">
    <div class="form-check me-2">
      <input class="form-check-input" type="checkbox"
             id="gap_on" name="gap_on"
             {% if settings.gap_on %}checked{% endif %}>
    </div>
    <label class="form-check-label text-warning d-flex align-items-center me-2" for="gap_on">
      Gap % ≥ 1.0 %
      <i class="bi bi-info-circle-fill ms-1 text-muted"
         data-bs-toggle="popover"
         title="Gap-up %"
         data-bs-content="(Open – PrevClose) ÷ PrevClose as a percentage.">
      </i>
    </label>
    <input type="hidden" name="gap_pct" value="1.0">
  </div>
</div>
</div>

  {# ─── 4) Exit & Risk Parameters ─── #}
  <div class="row g-3 mb-4 align-items-end">
    <div class="col-auto">
      <div class="form-check">
        <input class="form-check-input" type="checkbox"
               id="single_entry_only" name="single_entry_only"
               {% if settings.single_entry_only %}checked{% endif %}>
        <label class="form-check-label" for="single_entry_only">
          🛑 Single-Entry Only
        </label>
      </div>
    </div>
    <div class="col-auto">
      <div class="form-check">
        <input class="form-check-input" type="checkbox"
               id="use_trailing_stop" name="use_trailing_stop"
               {% if settings.use_trailing_stop %}checked{% endif %}>
        <label class="form-check-label" for="use_trailing_stop">
          🚧 Use Trailing Stop
        </label>
      </div>
    </div>
    <div class="col-auto">
      <label for="trailing_stop_pct" class="form-label">Stop %</label>
      <input type="number" step="0.01"
             id="trailing_stop_pct"
             name="trailing_stop_pct"
             class="form-control"
             value="{{ settings.trailing_stop_pct }}">
    </div>
    <div class="col-auto">
      <label for="sell_after_days" class="form-label">Sell After Days</label>
      <input type="number"
             id="sell_after_days"
             name="sell_after_days"
             class="form-control"
             value="{{ settings.sell_after_days or '' }}">
    </div>
<div class="col-auto">
  <label for="stop_loss_pct" class="form-label">Stop-Loss (%)</label>
  <input type="number" step="0.1" min="0" max="100"
         id="stop_loss_pct" name="stop_loss_pct"
         class="form-control"
         value="{{ (settings.stop_loss_pct * 100)|round(1) }}">
</div>


<div class="col-auto">
  <label for="take_profit_pct" class="form-label">Take-Profit (%)</label>
  <input type="number" step="0.1" min="0" max="100"
         id="take_profit_pct" name="take_profit_pct"
         class="form-control"
         value="{{ (settings.take_profit_pct * 100)|round(1) }}">
</div>

 
 </div>

{# ─── Money Management & Controls ─── #}
<form id="simulation-form"
      method="POST"
      action="{{ url_for('start_scanner') }}">

  {# …your existing indicator toggles and dropdowns here… #}

  <div class="row g-3 mb-4">
    <!-- Starting Cash -->
    <div class="col-md-3">
      <label for="starting_cash" class="form-label">
        <i class="bi bi-currency-dollar"></i> Starting Cash
      </label>
      <select id="starting_cash"
              name="starting_cash"
              class="form-select">
        {% for v in [1000, 5000, 10000, 25000, 50000] %}
          <option value="{{ v }}"
                  {% if settings.starting_cash == v %}selected{% endif %}>
            ${{ "{:,}".format(v) }}
          </option>
        {% endfor %}
      </select>
    </div>

    <!-- Max Per Trade -->
    <div class="col-md-3">
      <label for="max_per_trade" class="form-label">
        <i class="bi bi-arrow-up-right-circle"></i> Max Per Trade
      </label>
      <select id="max_per_trade"
              name="max_per_trade"
              class="form-select">
        {% for v in [100, 250, 500, 1000, 2500, 5000] %}
          <option value="{{ v }}"
                  {% if settings.max_per_trade == v %}selected{% endif %}>
            ${{ "{:,}".format(v) }}
          </option>
        {% endfor %}
      </select>
    </div>
  </div>

  <div class="d-flex justify-content-between align-items-start mb-4">
    <!-- Left: Section title -->
    <h3 class="text-white mb-0">Holdings</h3>

    <!-- Right: Status + Buttons -->
    <div class="d-flex flex-column align-items-end">
      <!-- 1) Dynamic status badge -->
      <span id="sim-status"
            class="badge bg-{{ 'success' if _is_scanner_running else 'secondary' }} mb-2">
        Status: <span id="sim-status-text">
                   {{ 'Running' if _is_scanner_running else 'Stopped' }}
                 </span>
      </span>

      <!-- 2) Start / Stop buttons -->
      <div>
        <!-- uses form’s default action (/start_scanner) -->
        <button type="submit"
                class="btn btn-success btn-sm me-2">
          ▶️ Start
        </button>
        <!-- overrides to call /stop_scanner -->
        <button type="submit"
                class="btn btn-danger btn-sm"
                formaction="{{ url_for('stop_scanner') }}">
          ⛔ Stop
        </button>
      </div>
    </div>
  </div>
</form>


  <!-- ─── Alerts Table ─── -->
  <div class="table-responsive">
    <table class="table table-striped table-bordered alerts-table text-white">
      <thead class="table-light">
        <tr>
          <th>Time</th><th>Symbol</th><th>Name</th><th>Price</th><th>VWAP</th><th>VWAP Diff</th><th>News</th><th>Spark</th><th>Triggers</th><th>Qty</th><th>Buy</th><th>Clear</th>
        </tr>
      </thead>
<tbody>
  {% for alert in alerts %}
  <tr data-id="{{ alert.id }}">
	<td>{{ alert.timestamp or "--" }}</td>
    <td>{{ alert.symbol }}</td>
    <td>{{ alert.name }}</td>
    <td>${{ "%.2f"|format(alert.price) }}</td>
    <td>${{ "%.2f"|format(alert.vwap) }}</td>
    <td>{{ alert.vwap_diff }}</td>
    <td>{{ alert.news or "—" }}</td>
	<td>{{ alert.sparkline|safe }}</td>
    <td>{{ alert.triggers }}</td>
    <td>
		  <input type="number" class="qty-input" min="1" value="1" />
		</td>
		<td>
		  <button
			class="btn btn-success btn-sm buy-btn"
			data-symbol="{{ alert.symbol }}"
		  >Buy</button>
		</td>
		<td>
		  <button
			class="btn btn-danger btn-sm clear-btn"
			data-id="{{ alert.id }}"
		  >Clear</button>
		</td>

  </tr>
      {% endfor %}
    </tbody>
    </table>
  </div>

  <!-- Replace this:
  <script src="{{ url_for('static', filename='js/simulation.js') }}"></script>
  -->
  <script src="{{ url_for('static', filename='js/alerts.js') }}"></script>
  <script>
  window.addEventListener('DOMContentLoaded',()=>{
    document.querySelectorAll('.indicator-setting').forEach(el=>{
      const key = el.name;
      // 1) On load, rehydr ate from localStorage if present
      const saved = localStorage.getItem(key);
      if (saved !== null) {
        if (el.type === 'checkbox') {
          el.checked = saved === '1';
        } else {
          el.value = saved;
        }
      }
      // 2) On each change, write back
      el.addEventListener('change', ()=>{
        if (el.type === 'checkbox') {
          localStorage.setItem(key, el.checked ? '1':'0');
        } else {
          localStorage.setItem(key, el.value);
        }
      });
    });
  });
</script>
  <!-- 1) Status poller -->
  <script>
    async function refreshStatus() {
      const res    = await fetch('/scanner_status');
      const { running } = await res.json();
      const badge  = document.getElementById('sim-status');
      const text   = document.getElementById('sim-status-text');

      if (running) {
        badge.classList.replace('bg-secondary', 'bg-success');
        text.textContent = 'Running';
      } else {
        badge.classList.replace('bg-success', 'bg-secondary');
        text.textContent = 'Stopped';
      }
    }

    // kick off immediately & then every 10 seconds
    refreshStatus();
    setInterval(refreshStatus, 10000);
  </script>

  <!-- 2) Bootstrap popovers -->
  <script>
    document
      .querySelectorAll('[data-bs-toggle="popover"]')
      .forEach(el => new bootstrap.Popover(el));
  </script>
{% endblock %}
