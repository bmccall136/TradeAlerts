{% extends "layout.html" %}
{% block content %}
<div class="container py-4">
  <h2 class="mb-4">Backtest Configuration &amp; Results</h2>

  <form method="GET" action="{{ url_for('backtest_view') }}" class="mb-4">
    <div class="row g-2 align-items-end">
      <!-- Indicator Toggles -->
      {% set toggles = [
        {'name':'bt_sma_on', 'label':'SMA',  'icon':'bi-graph-up-arrow'},
        {'name':'bt_rsi_on', 'label':'RSI',  'icon':'bi-speedometer2'},
        {'name':'bt_macd_on','label':'MACD','icon':'bi-triangle-fill'},
        {'name':'bt_bb_on',  'label':'BB',   'icon':'bi-bar-chart-fill'},
        {'name':'bt_vol_on','label':'Vol',  'icon':'bi-bar-chart-lines'},
        {'name':'bt_vwap_on','label':'VWAP','icon':'bi-calculator'},
        {'name':'bt_news_on','label':'News','icon':'bi-newspaper'}
      ] %}
      <div class="col-12 d-flex flex-wrap align-items-center mb-3">
        {% for t in toggles %}
          <div class="form-check form-switch me-3">
            <input class="form-check-input" type="checkbox" id="{{t.name}}" name="{{t.name}}" {% if bt_settings[t.name.replace('bt_','')] %}checked{% endif %}>
            <label class="form-check-label" for="{{t.name}}">
              <i class="bi {{t.icon}}"></i> {{t.label}}
            </label>
          </div>
        {% endfor %}
      </div>

      <!-- Numeric Filters -->
      {% set sma_labels = {10:'Short SMA (10)',20:'Standard SMA (20)',50:'Long SMA (50)',100:'100‑Day SMA (100)',200:'200‑Day SMA (200)'} %}
      <div class="col-6 col-md-2">
        <label for="bt_sma_len" class="form-label">SMA Length</label>
        <select id="bt_sma_len" name="bt_sma_len" class="form-select">
          {% for v,l in sma_labels.items() %}
            <option value="{{v}}" {% if bt_settings.sma_len==v %}selected{% endif %}>{{l}}</option>
          {% endfor %}
        </select>
      </div>

      {% set vwap_labels = {0.0:'No VWAP',0.5:'≥ $0.5',1.0:'≥ $1.0',2.0:'≥ $2.0'} %}
      <div class="col-6 col-md-2">
        <label for="bt_vwap_thr" class="form-label">VWAP Threshold</label>
        <select id="bt_vwap_thr" name="bt_vwap_thr" class="form-select">
          {% for v,l in vwap_labels.items() %}
            <option value="{{v}}" {% if bt_settings.vwap_thr==v %}selected{% endif %}>{{l}}</option>
          {% endfor %}
        </select>
      </div>

      <div class="col-auto ms-auto">
        <button type="submit" class="btn btn-primary">Run Backtest</button>
      </div>
    </div>
  </form>

  <!-- Results Table -->
  <div class="table-responsive mb-3">
    <table class="table table-striped backtest-table">
      <thead class="table-light">
        <tr><th>Date</th><th>Type</th><th>Price</th><th>Qty</th><th>P/L</th></tr>
      </thead>
      <tbody>
        {% for t in trades %}
        <tr>
          <td>{{ t.Date }}</td>
          <td>{{ t.Type }}</td>
          <td>${{ '%.2f'|format(t.Price) }}</td>
          <td>{{ t.Qty }}</td>
          <td>{% if t['P/L'] is not none %}${{ '%.2f'|format(t['P/L']) }}{% else %}&mdash;{% endif %}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  <div>
    <strong>Net Return:</strong> ${{ '%.2f'|format(pnl) }}
  </div>
</div>
{% endblock %}
