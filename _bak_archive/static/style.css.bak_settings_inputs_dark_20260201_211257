/*â€” Reset margins/padding, set black background, default font â€”*/
html, body {
    margin: 0;
    padding: 0;
    background-color: #000;   /* Black page background */
    color: #ffffff;           /* Default text color: white */
    font-family: Arial, sans-serif;
    line-height: 1.4;
}

/*â€” Links and buttons share the same font/size/reset â€”*/
a {
    color: #ffffff;
    text-decoration: none;
}
button,
input,
select {
    font-family: inherit;
    font-size: 1rem;
    line-height: 1.4;
    background: none;
    border: none;
    outline: none;
    cursor: pointer;
}
/* tooltip bubble */
.hint{
  display:inline-block;margin-left:.35rem;width:16px;height:16px;line-height:16px;
  text-align:center;border-radius:50%;font-weight:600;font-size:.75rem;background:#333;color:#bbb;
  cursor:help
}
.hint:hover{background:#444;color:#fff}
.num.pos, .alerts-table .pos { color:#39d353 !important; } /* green  */
.num.neg, .alerts-table .neg { color:#f05454 !important; } /* red    */
/* readable text on dark cards */
.card.bg-dark .kpi-card { color:#e9ecef; }          /* values */
.card.bg-dark .kpi-card .small { color:#cfd4da; }   /* labels */

/* keep sign colors working alongside the above */
.text-pos{color:#41d17d}.text-neg{color:#ff5c5c}.text-zero{color:#aaaaaa}
.kpi-cash-extra { font-variant-numeric: tabular-nums; }
/* kill accidental link styling inside the cards */
.kpi-card a { color:inherit; text-decoration:none; }
.kpi-card a:hover { text-decoration:underline; }

.pos { color: #39d353; }  /* green */
.neg { color: #f05454; }  /* red   */
/* readable text on dark KPI cards */
.card.bg-dark .kpi-card { color:#e9ecef; }          /* main numbers */
.card.bg-dark .kpi-card .small { color:#cfd4da; }   /* labels/subtext */

/* if anything shows as blue-underlined */
.kpi-card a { color:inherit; text-decoration:none; }
.kpi-card a:hover { text-decoration:underline; }

/* sign colors (already used by your JS) */
.text-pos{color:#41d17d}.text-neg{color:#ff5c5c}.text-zero{color:#aaaaaa}

/* Keep your sign colors working (no !important so they can be overridden if needed) */
.text-pos { color: #41d17d; }
.text-neg { color: #ff5c5c; }
.text-zero { color: #aaaaaa; }
/* readable text on dark cards */
.card.bg-dark .kpi-card,
.card.bg-dark .summary-card { color: #e9ecef; }

.card.bg-dark .kpi-card .small,
.card.bg-dark .summary-card .label { color: #cfd4da; }

/* if any old 'text-muted' remains inside these cards, brighten it */
.card.bg-dark .kpi-card .text-muted,
.card.bg-dark .summary-card .text-muted { color: #cfd4da !important; }

/* kill link styling inside these cards (no more blue + underline) */
.kpi-card a, .summary-card a { color: inherit; text-decoration: none; }
.kpi-card a:hover, .summary-card a:hover { text-decoration: underline; }

/* keep sign colors */
.text-pos { color:#41d17d; }
.text-neg { color:#ff5c5c; }
.text-zero{ color:#aaaaaa; }
/* P&L coloring for Live holdings & trades */
.text-pos {
  color: #00e676 !important;  /* green */
}

.text-neg {
  color: #ff5252 !important;  /* red */
}

.text-zero {
  color: #b8b8b8 !important;  /* neutral/gray */
}

/* P&L positive/negative coloring */
.pn-pos{color:#17c964}
.pn-neg{color:#f31260}
.num { min-width: 6ch; display: inline-block; text-align: right; }
.num:empty::before { content: 'â€”'; opacity: 0.55; }
.pn-pos { color: #55d17a; }
.pn-neg { color: #ff6b6b; }
.pn-pos { color:#16c784; }   /* green */
.pn-neg { color:#ff4d4d; }   /* red   */
.pn-flat{ color:#c9d1d9; }   /* neutral */
.kpi-card { font-size: 0.95rem; }
.kpi-card .kpi-title { font-size: 1rem; }
.table.alerts-table { font-size: 0.95rem; }
/* dark theme helpers */
.text-pos { color: #41d17d; }  /* green */
.text-neg { color: #ff5c5c; }  /* red */
.text-zero { color: #aaaaaa; } /* neutral */

/*â€” Container to center content and limit width â€”*/
.container {
    width: 99vw;
    max-width: 100vw;
    margin: 20px 0 0 0;   /* top margin, no auto-centering */
    padding: 0 8px;
    box-sizing: border-box;
}
.kpis{
  display:flex; flex-wrap:wrap; gap:.25rem .9rem;
  align-items:baseline; justify-content:center;
  margin:6px 0 10px; /* tighter vertical spacing */
}
.kpi{ display:flex; gap:.35rem; white-space:nowrap; font-size:.95rem; }
.kpi .label{ color:#bbb; }
.kpi.muted{ color:#777; }
@media (max-width:640px){ .kpis{ gap:.25rem .6rem; } .kpi{ font-size:.9rem; } }
.kpi-grid{
  display:grid;
  grid-template-columns:repeat(2, minmax(280px, 1fr));
  gap:14px;
  margin:6px 0 12px;
}

.kpi-card{
  background:rgba(255,255,255,0.03);
  border:1px solid rgba(255,255,255,0.08);
  border-radius:12px;
  padding:12px 14px;
}

.kpi-title{
  color:#f3c04d;           /* your gold */
  font-weight:600;
  font-size:1.15rem;       /* bigger title */
  margin-bottom:6px;
  letter-spacing:.02em;
}

.kv{
  display:flex;
  justify-content:space-between;
  align-items:baseline;
  gap:12px;
  padding:2px 0;
  font-size:1.08rem;       /* bump number size */
}

.kv .label{ color:#bdbdbd; }
.kv.muted { color:#7a7a7a; }
.kv span:last-child{ font-variant-numeric: tabular-nums; }

@media (max-width: 720px){
  .kpi-grid{ grid-template-columns:1fr; }
  .kv{ font-size:1rem; }
}


/* ===================================================================
   NAVIGATION BAR (ACROSS THE TOP)
   =================================================================== */

/*â€” Navbar wrapper: black background, fixed height, border bottom â€”*/
.navbar {
    background-color: #000;     /* black */
    border-bottom: 2px solid #222;
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: relative;
    height: 60px;
    padding: 0 20px;
}

/*â€” Left group of nav links â€”*/
.navbar-links {
    display: flex;
    gap: 20px;
    margin-left: 20px;
}
.nav-link {
    color: #ffe900;          /* bright yellow */
    font-weight: bold;
    font-size: 1rem;
    padding: 6px 10px;
    border-radius: 4px;
    transition: background 0.15s, color 0.15s;
}
.nav-link:hover,
.nav-link.active {
    background-color: #333;
    color: #fff;
}
/* ==============================================
   MAKE THE â€œNewsâ€ BUTTON VISIBLE
   ============================================== */
button.news-btn {
  /* Show a small border on black background so itâ€™s easy to click */
  border: 1px solid #ffe900;   /* bright yellow outline */
  background: transparent;     /* keep no background fill */
  color: #ffe900;              /* bright yellow text */
  font-size: 0.9rem;           /* make it readable */
  padding: 4px 8px;            /* give it some clickable area */
  border-radius: 4px;
  cursor: pointer;
}

button.news-btn:hover {
  background-color: rgba(255, 233, 0, 0.2);  /* faint yellowish hover */
  color: #ffffff;                            /* white on hover */
}

/* =============================================================================
   Enlarge the TradeAlerts title in the navbar
   ============================================================================= */
.trade-alerts-title {
  font-size: 1.75rem;   /* about 28px on most screens */
  letter-spacing: 1px;  /* a little breathing room between letters */
  color: #ffe900;       /* keep it bright yellow */
  text-shadow: 1px 1px 2px rgba(0,0,0,0.7);
  /* center positioning is already handled by Bootstrap classes */
}

/*â€” Rightâ€side shortcut buttons (Auth / Nuke DB / Clear All) â€”*/
.navbar-shortcuts {
    display: flex;
    gap: 10px;
    margin-left: auto;
}
.clear-all-btn,
.auth-btn,
.nuke-btn {
    background-color: #111;      /* dark gray */
    color: #ffe900;              /* yellow text */
    border: 2px solid #ffe900;   /* yellow border */
    padding: 6px 14px;
    border-radius: 6px;
    font-weight: bold;
    font-size: 0.9rem;
    transition: background 0.15s, color 0.15s;
    white-space: nowrap;
}
.nuke-btn {
    border-color: #ff4444;       /* bright red border */
    color: #ff4444;
}
.clear-all-btn:hover,
.auth-btn:hover,
.nuke-btn:hover {
    background-color: #ffe900;   /* yellow on hover */
    color: #000;                 /* black text on hover */
}
.nuke-btn:hover {
    background-color: #660000;   /* deep red on hover */
    color: #fff;
}

/* ===================================================================
   GENERIC BUTTON STYLES (used across pages)
   =================================================================== */

/*â€” â€œClear Allâ€ or â€œClearâ€ buttons (Alerts page) â€”*/
button.btn-clear {
    background-color: #ff5151;   /* bright red */
    color: #ffffff;
    padding: 6px 12px;
    border-radius: 4px;
    font-weight: bold;
    font-size: 0.9rem;
    transition: background 0.15s;
}
button.btn-clear:hover {
    background-color: #e04444;
}

/*â€” â€œBuyâ€ buttons in Alerts table â€”*/
button.btn-buy {
    background-color: #10ff50 !important;  /* force bright green */
    color: #000000 !important;             /* force black text */
    padding: 6px 12px;
    border-radius: 4px;
    font-weight: bold;
    font-size: 0.9rem;
    cursor: pointer;
    transition: background 0.15s;
}
button.btn-buy:hover {
    background-color: #0ec240 !important;  /* slightly darker green on hover */
}
/* Simulation / Alerts: horizontal scroll */
.alerts-table {
  width: 100%;
  border-collapse: collapse;
  table-layout: fixed; /* â†’ allows horizontal scrolling */
}
.alerts-table th,
.alerts-table td {
  padding: 8px 12px;
  color: #ffff66; /* light yellow text on black background */
  font-size: 0.9rem;
  border-bottom: 1px solid #333;
  white-space: nowrap; /* prevents wrapping */
}

/*â€” Table header styling (yellow text) â€”*/
.alerts-table thead {
    background-color: #000;
}

/*â€” Table body cells (white text by default) â€”*/
.alerts-table tbody tr {
    background-color: #000;
    border-bottom: 1px solid #111;
}

/* Red â€œSellâ€ button: */
button.btn-clear {
  background-color: #ff5151 !important;  /* bright red */
  color: #fff !important;
  padding: 6px 10px;
  border: none;
  border-radius: 4px;
  font-size: 0.9rem;
  font-weight: bold;
  cursor: pointer;
  margin-left: 8px;
  transition: background 0.15s;
}
button.btn-clear:hover {
  background-color: #e04444 !important;
}

/* Green â€œBuyâ€ button: */
button.btn-buy {
  background-color: #10ff50 !important;  /* bright green */
  color: #000 !important;
  padding: 6px 10px;
  border: none;
  border-radius: 4px;
  font-size: 0.9rem;
  font-weight: bold;
  cursor: pointer;
  margin-left: 8px;
  transition: background 0.15s;
}
button.btn-buy:hover {
  background-color: #0ec240 !important;
}

/* â€¦ rest of your CSS â€¦ */


/* ===================================================================
   ALERTS TABLE (used on Alerts page, and as base for Simulation)
   =================================================================== */

/*â€” Base table container â€”*/
.alerts-table {
    width: 100%;
    border-collapse: collapse;
    background: #000;            /* black background */
    margin-bottom: 20px;
}
.alerts-table {
    table-layout: auto !important;
}

/*â€” Table header styling (yellow text) â€”*/
.alerts-table thead {
    background-color: #000;
}
.alerts-table thead th {
    padding: 10px 12px;
    text-align: left;
    border-bottom: 2px solid #222;
    color: #ffe900 !important;   /* bright yellow */
    font-size: 1.05rem;
    white-space: nowrap;
}

/*â€” Table body cells (yellow text by default) â€”*/
.alerts-table tbody tr {
    background-color: #000;
    border-bottom: 1px solid #111;
}
.alerts-table tbody td {
    padding: 8px 12px;
    text-align: left;
    color: #ffffff !important;   /* white text */
    font-size: 0.95rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

/*â€” Highlight on hover â€”*/
.alerts-table tbody tr:hover {
    background-color: #222222;
}

/*â€” Sparkline images on Alerts page â€”*/
.sparkline-img {
    width: 100px;
    height: 30px;
}

/* ===================================================================
   TRADE HISTORY TABLE (used on Simulation & Backtest pages)
   =================================================================== */

.trade-history-table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 40px;
}
.trade-history-table thead th {
    color: #ffffff;
    border-bottom: 1px solid #555555;
    padding: 10px 12px;
    text-align: left;
}
.trade-history-table tbody tr td {
    color: #ffffff;
    padding: 8px 12px;
    border-bottom: 1px solid #333333;
    text-align: left;
    font-size: 0.95rem;
}

/* ===================================================================
   SIMULATION PAGE BASE STYLES (BEFORE OVERRIDES)
   =================================================================== */

/*â€” Header + â€œReset Simulationâ€ button â€”*/
.simulation-header {
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 20px 0;
}
.simulation-header h1 {
    color: #ffffff;
    margin-right: 20px;
    font-size: 2rem;
}
/* Ensure the â€œReset Simulationâ€ button itself matches your theme */
.reset-btn {
  background-color: #ffd700;
  color: #000;
  font-weight: bold;
  padding: 6px 12px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
.reset-btn:hover {
  background-color: #e6c200;
}

/*â€” Cash / P&L summary lines â€”*/
.simulation-summary {
    text-align: center;
    margin-bottom: 20px;
}
.simulation-summary p {
    color: #ffffff;
    font-size: 1.1rem;
    margin: 4px 0;
}

/*â€” â€œHoldingsâ€ heading â€”*/
h2 {
    color: #ffffff;
    margin-top: 20px;
    margin-bottom: 10px;
}

/*â€” Quantity input inside the holdings table â€”*/
.qty-input {
    border: 1px solid #444;
    border-radius: 5px;
    padding: 4px 8px;
    background: #111;
    color: #ffe900;
    width: 60px;
    text-align: center;
    margin-right: 8px;
}

/*â€” In case .sell-btn-red fails, fallback â€œSellâ€ styling (gold) â€”*/
.sell-btn {
    background-color: #d4af37;  /* gold */
    border: none;
    color: #000;
    padding: 4px 8px;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.9rem;
    margin-left: 8px;
    transition: background 0.15s;
}
.sell-btn:hover {
    background-color: #b99a2e;
}

/* ===================================================================
   SIMULATION PAGE OVERRIDES (CLEAN & FINAL)
   =================================================================== */

/*â€” 1) Reset the Alertsâ€‘table nthâ€‘child widths for Simulation â€”*/
.alerts-table.simulation-table th:nth-child(1),
.alerts-table.simulation-table td:nth-child(1),
.alerts-table.simulation-table th:nth-child(2),
.alerts-table.simulation-table td:nth-child(2),
.alerts-table.simulation-table th:nth-child(3),
.alerts-table.simulation-table td:nth-child(3),
.alerts-table.simulation-table th:nth-child(4),
.alerts-table.simulation-table td:nth-child(4),
.alerts-table.simulation-table th:nth-child(5),
.alerts-table.simulation-table td:nth-child(5),
.alerts-table.simulation-table th:nth-child(6),
.alerts-table.simulation-table td:nth-child(6),
.alerts-table.simulation-table th:nth-child(7),
.alerts-table.simulation-table td:nth-child(7),
.alerts-table.simulation-table th:nth-child(8),
.alerts-table.simulation-table td:nth-child(8),
.alerts-table.simulation-table th:nth-child(9),
.alerts-table.simulation-table td:nth-child(9),
.alerts-table.simulation-table th:nth-child(10),
.alerts-table.simulation-table td:nth-child(10) {
    width: auto !important;
}

/*â€” 2) Make the <colgroup> percentages work exactly for Simulation table â€”*/
.simulation-table {
    table-layout: fixed !important;
    width: 100% !important;
}

/*â€” 3) Force all nonâ€‘â€œDay Gainâ€ cells white (override any inherited yellow) â€”*/
.simulation-table tbody tr td {
    color: #ffffff !important;
}

/*â€” 4) Day Gain cells: bright green if positive, bright red if negative â€”*/
/* Day Gain/Loss coloring */
.gain-positive {
  color: #10ff50 !important;  /* bright green */
  font-weight: bold;
}
.gain-negative {
  color: #ff5151 !important;  /* bright red */
  font-weight: bold;
}
/*â€” 5) Sell button: bright red background, white text, forced by !important â€”*/
.sell-btn-red {
    background-color: #ff5151 !important;
    border: none !important;
    color: #ffffff !important;
    padding: 4px 8px !important;
    border-radius: 4px !important;
    cursor: pointer !important;
    font-size: 0.9rem !important;
    margin-left: 8px !important;
    transition: background 0.15s !important;
}
.sell-btn-red:hover {
    background-color: #e04444 !important;  /* darker red on hover */
}
/* =============================================================================
   FORCE FILTERâ€BAR & FORM CONTROLS TO BE BLACK ON DARK THEME
   ============================================================================= */

/* 1) Make <input> and <select> fields in filter bar black background, yellow text */
.row .form-control,
.row .form-select {
  background-color: #000 !important;      /* pure black */
  color: #ffe900 !important;              /* bright yellow */
  border: 1px solid #444 !important;      /* dark border so you can see edges */
  box-shadow: none !important;
}

/* 2) Force the dropdown caret (on .form-select) to be white/yellow so itâ€™s visible */
.row .form-select {
  /* override Bootstrapâ€™s default blue caret */
  -webkit-text-fill-color: #ffe900 !important;
  -moz-text-fill-color: #ffe900 !important;
}
.row .form-select::placeholder {
  color: #999;    /* if you have a â€œLengthâ€ placeholder, keep it gray */
}

/* 3) When a <select> is â€œfocused,â€ keep border bright yellow */
.row .form-select:focus,
.row .form-control:focus {
  outline: none !important;
  border-color: #ffe900 !important;       /* yellow on focus */
  box-shadow: none !important;
  background-color: #000 !important;
  color: #ffe900 !important;
}

/* 4) The <button class="btn btn-success"> â€œApplyâ€ is green; override its text to white */
.btn-success {
  color: #ffffff !important;
}

/* 5) Table header (<thead>) â€“ force black background, yellow text */
.alerts-table thead {
  background-color: #000 !important;
}
.alerts-table thead th {
  background-color: #000 !important;
  color: #ffe900 !important;   /* bright yellow */
}

/* 6) Table body (<tbody>) â€“ ensure background stays black, and text is white or yellow */
.alerts-table tbody tr {
  background-color: #000 !important;
}
.alerts-table tbody tr td {
  background-color: #000 !important;
  color: #ffffff !important;   /* pure white text, except where you override (e.g., vwap_diff) */
}

/* 7) If you wrapped your table in .table-responsive, force that container to remain black */
.table-responsive {
  background-color: #000 !important;
}

/* 8) If any Bootstrap â€œ.bg-lightâ€ got injected, turn it back to black */
.bg-light {
  background-color: #000 !important;
  color: #ffffff !important;
}

/* 9) OPTIONAL: Darken the scrollbar (if you want to hide the default white scrollbar on wide tables) */
.table-responsive::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}
.table-responsive::-webkit-scrollbar-track {
  background: #111;
}
.table-responsive::-webkit-scrollbar-thumb {
  background-color: #333;
  border-radius: 4px;
}
.table-responsive::-webkit-scrollbar-thumb:hover {
  background-color: #555;
}

/*â€” 6) Tighten padding in Simulation table cells (so columns do not overflow) â€”*/
.simulation-table th,
.simulation-table td {
    padding: 6px 8px !important;
    text-align: center !important;
}

/*â€” 7) Reduce right padding on the â€œActionâ€ column so Sell button never clips â€”*/
.simulation-table td:last-child {
    padding-right: 4px !important;
}
/* =============================================================================
   Darken the table borders (no more bright white grid lines)
   ============================================================================= */

/* 1) Make the table outer border dark instead of white */
.alerts-table,
.alerts-table th,
.alerts-table td {
  border-color: #333 !important;      /* very dark gray */
}

/* 2) If Bootstrap added a white background inside <thead> or <tbody>, force them black */
.alerts-table thead th,
.alerts-table tbody td {
  background-color: transparent !important;
}

/* 3) Changing the horizontal â€œhrâ€ line under the navbar to a darker gray (if you have one) */
.navbar + hr {
  border-color: #333 !important;
}

/* 4) If you have any â€œ.tableâ€ class defaults leftover, force that to no border or dark border */
.table-bordered {
  border: 1px solid #333 !important;
}
.filter-toggle-group {
  display: flex !important;
  align-items: center;
  gap: 1rem;
  overflow-x: auto;
  white-space: nowrap;
  margin-bottom: 1rem;
}
.btn-warning {
    background-color: #ffc107;
    color: black;
    padding: 8px 12px;
    border-radius: 5px;
    text-decoration: none;
    margin-left: 5px;
    font-weight: bold;
}
.btn-fixed {
    width: 120px;         /* Adjust width to match Nuke DB exactly */
    padding: 4px 6px;     /* Match small button padding */
    font-size: 0.85rem;   /* Small font size like btn-sm */
    text-align: center;
    vertical-align: middle;
    font-weight: bold;
    display: inline-block;
    white-space: nowrap;  /* Prevents wrapping text */
    overflow: hidden;     /* Keeps overflow hidden neatly */
    text-overflow: ellipsis; /* Adds ellipsis if too long */
}
/* Center the hero blurb under the header */
.hero-blurb {
  max-width: 980px;
  margin: 8px auto 22px;   /* auto centers in non-flex too */
  padding: 0 12px;         /* a little breathing room on narrow screens */
}
/* Header Checkpoint button */
.btn-checkpoint {
  font-size: 0.9rem;
  font-weight: 600;
  color: #ffd54f !important;        /* bright yellow text */
  border: 1px solid #ffd54f;        /* yellow outline */
  background-color: #111111;        /* blend with dark header */
  padding: 4px 10px;
  border-radius: 4px;
}

.btn-checkpoint:hover {
  background-color: #ffd54f;        /* invert on hover */
  color: #000000 !important;
}
/* Tiny news button */
.btn-news {
  padding: 2px 6px;
  font-size: 0.75rem;
  border-radius: 3px;
  border: 1px solid #ffeb3b;
  background-color: #111111;
  color: #ffeb3b;
  text-decoration: none;
}

.btn-news:hover {
  background-color: #ffeb3b;
  color: #000000;
  text-decoration: none;
}
.ai-toggle {
  display: inline-flex;
  align-items: center;
  margin-left: 1rem;
}

.ai-toggle-label {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  cursor: pointer;
  font-size: 0.9rem;
}

.ai-toggle-label input[type="checkbox"] {
  transform: scale(1.1);
}

.ai-icon {
  width: 18px;
  height: 18px;
  filter: drop-shadow(0 0 2px rgba(0, 255, 255, 0.7));
}

/* Max badge sizing */
.mm-gear{height:44px;width:auto;max-width:120px;object-fit:contain;vertical-align:middle}

/* Holdings table: make headers line up with numbers */
#holdings-body td,
.table thead th {
  white-space: nowrap;
  vertical-align: middle;
}

/* Align numeric columns right for BOTH th + td */
.table thead th:nth-child(3),
.table thead th:nth-child(4),
.table thead th:nth-child(5),
.table thead th:nth-child(6),
.table thead th:nth-child(7),
.table thead th:nth-child(8),
.table thead th:nth-child(9),
.table thead th:nth-child(10),
.table thead th:nth-child(11) {
  text-align: right;
}

#holdings-body td:nth-child(3),
#holdings-body td:nth-child(4),
#holdings-body td:nth-child(5),
#holdings-body td:nth-child(6),
#holdings-body td:nth-child(7),
#holdings-body td:nth-child(8),
#holdings-body td:nth-child(9),
#holdings-body td:nth-child(10),
#holdings-body td:nth-child(11) {
  text-align: right;
}

/* Symbol + Opened left */
.table thead th:nth-child(1),
.table thead th:nth-child(2),
#holdings-body td:nth-child(1),
#holdings-body td:nth-child(2) {
  text-align: left;
}

/* News centered */
.table thead th:nth-child(12),
#holdings-body td:nth-child(12) {
  text-align: center;
}

/* Consistent column widths so headers visually align */
.table {
  table-layout: fixed;
  width: 100%;
}
.table thead th:nth-child(1)  { width: 6rem; }   /* Symbol */
.table thead th:nth-child(2)  { width: 12rem; }  /* Opened */
.table thead th:nth-child(3)  { width: 7rem; }   /* Last */
.table thead th:nth-child(4)  { width: 4rem; }   /* Qty */
.table thead th:nth-child(5)  { width: 7rem; }   /* Paid */
.table thead th:nth-child(6)  { width: 7rem; }   /* Diff */
.table thead th:nth-child(7)  { width: 7rem; }   /* Day P&L */
.table thead th:nth-child(8)  { width: 6rem; }   /* Day % */
.table thead th:nth-child(9)  { width: 7rem; }   /* Total P&L */
.table thead th:nth-child(10) { width: 6rem; }   /* Total % */
.table thead th:nth-child(11) { width: 7rem; }   /* Value */
.table thead th:nth-child(12) { width: 5rem; }   /* News */

/* Analytics gain coloring */
td.ax-num.ax-pos, .ax-pos { color: #16a34a !important; font-weight: 700; }
td.ax-num.ax-neg, .ax-neg { color: #dc2626 !important; font-weight: 700; }
td.ax-num.ax-zero, .ax-zero { color: #a3a3a3 !important; font-weight: 700; }


/* === TRADE_REVIEW_CHART_FIT_OVERRIDE === */
/* Make the trade review chart fully visible (no clipping) */
.trade-chart-wrap,
.trade-chart-panel,
.trade-review-chart,
#trade-review-chart,
#tradeChartWrap,
#tradeChartPanel {
  height: auto !important;
  min-height: 0 !important;
  overflow: visible !important;
}

.trade-chart-wrap img,
.trade-chart-panel img,
.trade-review-chart img,
#trade-review-chart img,
#tradeChartWrap img,
#tradeChartPanel img {
  display: block !important;
  width: 100% !important;
  height: auto !important;
  max-height: none !important;
  object-fit: contain !important;
}


/* ================================
   BEGIN SETTINGS THEME OVERRIDES
   Make Settings match $$Machine UI
   ================================ */

#settings-page {
  /* full width like Dashboard */
  width: 100%;
  max-width: none;
  margin: 0;
}

/* Cards/panels */
#settings-page .card {
  background: rgba(255,255,255,0.10) !important;
  border: 1px solid rgba(255,255,255,0.16) !important;
  border-radius: 14px !important;
  color: rgba(255,255,255,0.95) !important;
}

/* Headings */
#settings-page h2,
#settings-page h5 {
  color: #ffe900 !important;
}

/* Muted text should stay readable */
#settings-page .text-muted {
  color: rgba(255,255,255,0.60) !important;
}

/* Tables: remove bootstrap "blue dark" tint */
#settings-page .table,
#settings-page .table-dark {
  --bs-table-bg: transparent;
  background: transparent !important;
}

#settings-page .table thead th {
  background: rgba(0,0,0,0.60) !important;
  border-bottom: 1px solid rgba(255,255,255,0.10) !important;
}

#settings-page .table tbody tr {
  background: rgba(255,255,255,0.03) !important;
}

#settings-page .table td,
#settings-page .table th {
  border-color: rgba(255,255,255,0.08) !important;
}

/* Inputs/selects: match Dashboard dark controls */
#settings-page input[type="text"],
#settings-page input[type="number"],
#settings-page select,
#settings-page .form-select,
#settings-page .form-control {
  background: rgba(0,0,0,0.65) !important;
  color: rgba(255,255,255,0.92) !important;
  border: 1px solid rgba(255,255,255,0.18) !important;
}

#settings-page input[type="text"]::placeholder,
#settings-page input[type="number"]::placeholder {
  color: rgba(255,255,255,0.35) !important;
}

#settings-page input:focus,
#settings-page select:focus,
#settings-page .form-select:focus,
#settings-page .form-control:focus {
  outline: none !important;
  box-shadow: none !important;
  border-color: #ffe900 !important;
}

/* Buttons: keep yellow pop, tighten hover */
#settings-page .btn-warning {
  background: #ffe900 !important;
  border-color: #ffe900 !important;
  color: #000 !important;
}

#settings-page .btn-warning:hover {
  filter: brightness(0.92);
}

/* Checkbox look */
#settings-page input[type="checkbox"] {
  transform: scale(1.05);
  accent-color: #ffe900;
}

/* ================================
   END SETTINGS THEME OVERRIDES
   ================================ */


#settings-page .table tbody tr { background: rgba(255,255,255,0.08) !important; }
#settings-page .table td, #settings-page .table th { color: rgba(255,255,255,0.95) !important; }
#settings-page .table thead th { background: rgba(255,255,255,0.06) !important; color: rgba(255,255,255,0.98) !important; }

#settings-page input[type="text"],
#settings-page input[type="number"],
#settings-page select,
#settings-page .form-select,
#settings-page .form-control {
  background: rgba(255,255,255,0.14) !important;
  color: rgba(255,255,255,0.98) !important;
  border: 1px solid rgba(255,255,255,0.22) !important;
}

#settings-page .table tbody tr { background: rgba(255,255,255,0.08) !important; }
#settings-page .table td, #settings-page .table th { color: rgba(255,255,255,0.95) !important; }
#settings-page .table thead th { background: rgba(255,255,255,0.06) !important; color: rgba(255,255,255,0.98) !important; }

#settings-page input[type="text"],
#settings-page input[type="number"],
#settings-page select,
#settings-page .form-select,
#settings-page .form-control {
  background: rgba(255,255,255,0.14) !important;
  color: rgba(255,255,255,0.98) !important;
  border: 1px solid rgba(255,255,255,0.22) !important;
}
